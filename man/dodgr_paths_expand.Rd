% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{dodgr_paths_expand}
\alias{dodgr_paths_expand}
\title{Expand a contracted path back on to the full, uncontracted graph.}
\usage{
dodgr_paths_expand(path_c, graph, graph_c, edge_map = NULL)
}
\arguments{
\item{path_c}{A single path along a contracted graph, generally
generated by calling \link{dodgr_paths} on a graph previously contracted
with \link{dodgr_contract_graph}. May be given either as a character
vector of vertex IDs (as returned by \link{dodgr_paths} with \code{vertices = TRUE}), or as an integer vector of row indices into \code{graph_c}.}

\item{graph}{The full, uncontracted graph from which \code{graph_c} was
generated with \link{dodgr_contract_graph}.}

\item{graph_c}{The contracted graph on which \code{path_c} was calculated.}

\item{edge_map}{Optional edge map generated internally by
\link{dodgr_contract_graph}, and used to map contracted edges back on to
their corresponding sequences of edges in \code{graph}. If not given, this is
extracted directly from the dodgr cache.}
}
\value{
A \code{data.frame} of the rows of \code{graph} corresponding to the full,
uncontracted sequence of edges traced by \code{path_c}, ordered from start to
end of the path.
}
\description{
A path calculated with \link{dodgr_paths} on a graph contracted with
\link{dodgr_contract_graph} traces only the intervening junction vertices,
skipping any intermediate (straight-line) vertices removed during
contraction. This function expands such a path back into the full sequence
of edges of the original, uncontracted graph.
}
\examples{
graph <- weight_streetnet (hampi)
graph_c <- dodgr_contract_graph (graph)
verts <- dodgr_vertices (graph_c)
from <- verts$id [1]
to <- verts$id [nrow (verts)]
path_c <- dodgr_paths (graph_c, from = from, to = to) [[1]] [[1]]
path <- dodgr_paths_expand (path_c, graph, graph_c)
}
\seealso{
Other distances:
\code{\link[=dodgr_distances]{dodgr_distances()}},
\code{\link[=dodgr_dists]{dodgr_dists()}},
\code{\link[=dodgr_dists_categorical]{dodgr_dists_categorical()}},
\code{\link[=dodgr_dists_nearest]{dodgr_dists_nearest()}},
\code{\link[=dodgr_paths]{dodgr_paths()}},
\code{\link[=dodgr_times]{dodgr_times()}}
}
\concept{distances}
