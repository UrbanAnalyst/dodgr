CXX_STD = CXX11
PKG_CPPFLAGS=-I.

PKG_LIBS += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
PKG_LIBS += $(shell ${R_HOME}/bin/Rscript -e "RcppParallel::RcppParallelLibs()")

SOURCES = $(wildcard heaps/*.cpp *.cpp)
OBJECTS = $(SOURCES:.cpp=.o)

all: $(SHLIB)

clean: $(NAME) --directory=heaps distclean
